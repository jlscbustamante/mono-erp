export interface IParametersApp {
  detraction_meat: {
    percentage: number;
    messsages: Record<string, string>;
  };
  moturider: {
    cia: number;
  };
  job_titles_ids: {
    delivery: number;
  };
}
