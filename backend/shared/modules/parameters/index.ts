export interface IParametersApp {
  detraction_meat: {
    percentage: number;
    messsage: {
      "Producto sujeto detraccion": string;
      "Cuenta detraccion": string;
    };
  };
  moturider: {
    cia_raul: number;
  };
  job_titles: {
    delivery: number;
  };
}
