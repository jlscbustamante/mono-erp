export enum CashAccountStatus {
  Active = 1,
  Inactive = 0,
}

export enum CashAccountTypeStatus {
  Active = 'A',
  Inactive = 'E',
}

export enum CashAccountTypeId {
  Store = 1,
  Bank = 3,
  Temporal = 7,
  Liquidator = 6,
  Corales = 5,
  Central = 4,
}
